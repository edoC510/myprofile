id: cis-ubuntu-20.04-2.1.1
os: ubuntu-20.04
title: Ensure xinetd is not installed
benchmark: CIS Ubuntu 20.04 LTS
level: 1
description: Ensure xinetd is not installed or is disabled
needs_sudo: false
check:
  bash: |
    ! dpkg -s xinetd 2>/dev/null | grep -q "Status: install"
references:
  - https://ubuntu.com/security/cis
controls:
  - CIS 2.1.1

