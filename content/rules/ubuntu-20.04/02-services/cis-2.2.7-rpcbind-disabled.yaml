id: cis-ubuntu-20.04-2.2.7
os: ubuntu-20.04
title: Ensure rpcbind is not installed
benchmark: CIS Ubuntu 20.04 LTS
level: 1
description: Ensure rpcbind is not installed or is disabled
needs_sudo: false
check:
  bash: |
    ! systemctl is-enabled rpcbind 2>/dev/null | grep -q enabled
references:
  - https://ubuntu.com/security/cis
controls:
  - CIS 2.2.7

